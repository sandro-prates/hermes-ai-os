# Hermes AI OS — Project Handoff

## 1. Estado Geral

* **Projeto:** Hermes AI OS
* **Versão:** `0.0.1`
* **Branch:** `main`
* **Upstream:** `origin/main`
* **Último commit:** `2a30fa443977b8687da9574538f2f554bfe18008` (`2a30fa4`) — `feat(core): add API v1, settings and health endpoint`
* **Commit anterior:** `feec40af0d43e26510fd626ff9260e317ccbf0ec` (`feec40a`) — `chore: bootstrap Hermes AI OS foundation`
* **Ambiente da aplicação:** `development`
* **Runtime verificado:** Windows, PowerShell 5.1, ambiente virtual `.venv`, Python `3.14.6`
* **Fase declarada no PROJECT_MASTER:** M0 — Foundation
* **Trabalho atual declarado:** EPIC-003 — Logging System / SPRINT-02 — Em andamento
* **Estado do repositório:** working tree com alterações locais; a SPRINT-02 não está commitada
* **Validações verificadas:** importação da aplicação aprovada; health endpoint HTTP 200; `git diff --check` sem erros
* **Qualidade verificada:** `pytest` coletou 0 testes; Ruff encontrou 4 violações `I001`
* **Estado exato do staging após os últimos ajustes em `docs/00_PROJECT_MASTER.md` e `docs/01_PROJECT_STATE.yaml`:** Não identificado

## 2. Arquitetura Atual

A arquitetura implementada e identificada no código é:

1. **Aplicação FastAPI**

   * Entrada em `apps/backend/app/main.py`.
   * Metadados configurados por `APP_NAME` e `APP_VERSION`.
   * Router da API registrado no FastAPI.
   * Middleware de logging registrado no FastAPI.

2. **Camada de API**

   * Router principal em `apps/backend/app/api/router.py`.
   * Prefixo `/api/v1`.
   * Endpoint de health em `apps/backend/app/api/v1/health.py`.
   * Endpoint raiz em `apps/backend/app/main.py`.

3. **Configuração central**

   * `pydantic-settings` em `apps/backend/app/core/settings.py`.
   * Leitura de `.env`.
   * Configuração case-insensitive.
   * Campos identificados:

     * `APP_NAME`
     * `APP_VERSION`
     * `ENVIRONMENT`
     * `DEBUG`
     * `HOST`
     * `PORT`
     * `LOG_LEVEL`
     * `LOG_FORMAT`
     * `REQUEST_ID_HEADER`

4. **Observabilidade**

   * Pacote ativo: `app.core.observability`.
   * Configuração com `logging.config.dictConfig`.
   * Logger com namespace `hermes`.
   * Handler de console em `stdout`.
   * `ConsoleFormatter` para logs legíveis.
   * `RequestContextFilter` para injetar `request_id`.
   * `ContextVar` para contexto assíncrono.
   * Restauração do contexto com `Token`.
   * Middleware ASGI puro.
   * Geração e propagação de `X-Request-ID`.
   * Registro de método, caminho, status HTTP e duração.
   * Registro de início, conclusão e exceção de requisições.

5. **Configuração de projeto Python**

   * `pyproject.toml` existe localmente e é parseável.
   * Build backend: `setuptools.build_meta`.
   * Configuração central de dependências, pytest e Ruff.
   * O arquivo ainda não está commitado.

6. **Continuidade documental**

   * `docs/00_PROJECT_MASTER.md`
   * `docs/01_PROJECT_STATE.yaml`
   * `docs/02_BACKLOG.md`
   * `docs/03_CHANGELOG.md`
   * `docs/adr/`
   * Os documentos foram criados e validados em UTF-8.
   * O commit da documentação ainda não foi comprovado.

Não foram identificadas implementações funcionais de banco de dados, runtime de agentes, memória, dashboard, model router, marketplace ou recursos Enterprise.

## 3. Funcionalidades Implementadas

Somente funcionalidades verificadas no código e/ou por execução:

* Aplicação FastAPI importa sem erro (`IMPORT_OK`).
* Endpoint raiz `GET /` retorna:

  * projeto;
  * versão;
  * ambiente;
  * status `running`.
* API v1 registrada sob `/api/v1`.
* Endpoint `GET /api/v1/health` retorna HTTP 200.
* Health endpoint retorna:

  * `status: healthy`;
  * nome do projeto;
  * versão;
  * ambiente.
* Settings centralizados carregam variáveis do arquivo `.env`.
* Logging central usa `dictConfig`.
* Logs usam o namespace `hermes`.
* Logs de console exibem timestamp, nível, logger, Request ID e mensagem.
* Middleware gera `X-Request-ID` quando o cliente não envia um valor.
* Middleware preserva `X-Request-ID` recebido do cliente.
* Middleware adiciona `X-Request-ID` à resposta.
* O mesmo Request ID aparece no header e nos logs.
* Logs HTTP exibem método, caminho, status e duração.
* Requisição manual ao health endpoint foi medida em aproximadamente `0.002054` segundos.
* Middleware restaura o `ContextVar` ao final da requisição.
* Documentos de continuidade existem e foram validados quanto a UTF-8, YAML e correspondência com a base Git `2a30fa4`.

O caminho de exceção do middleware existe no código, mas sua execução não foi comprovada por teste automatizado ou teste manual registrado.

## 4. Estrutura de Pastas

```text
Hermes-AI-OS/
├── .editorconfig
├── .env
├── .env.example
├── .gitignore
├── Hermes-AI-OS.code-workspace
├── LICENSE
├── README.md
├── pyproject.toml
├── apps/
│   └── backend/
│       └── app/
│           ├── main.py
│           ├── api/
│           │   ├── __init__.py
│           │   ├── router.py
│           │   └── v1/
│           │       ├── __init__.py
│           │       └── health.py
│           ├── core/
│           │   ├── logging.py
│           │   ├── settings.py
│           │   └── observability/
│           │       ├── __init__.py
│           │       ├── constants.py
│           │       ├── filters.py
│           │       ├── formatters.py
│           │       ├── logging.py
│           │       ├── middleware.py
│           │       └── request_context.py
│           ├── models/
│           └── services/
├── docs/
│   ├── 00_PROJECT_MASTER.md
│   ├── 01_PROJECT_STATE.yaml
│   ├── 02_BACKLOG.md
│   ├── 03_CHANGELOG.md
│   ├── adr/
│   │   ├── README.md
│   │   ├── ADR-0001-pyproject-como-fonte-de-dependencias.md
│   │   ├── ADR-0002-pacote-central-de-observabilidade.md
│   │   ├── ADR-0003-middleware-asgi-e-contextvars.md
│   │   └── ADR-0004-documentacao-como-sistema-de-continuidade.md
│   └── research/
│       └── 2026-07-12-stack-tecnologica.md
└── tests/
```

Observações verificadas:

* `tests/` não contém testes coletáveis.
* `README.md` está vazio.
* `docs/research/2026-07-12-stack-tecnologica.md` está vazio.
* Conteúdo implementado em `models/` e `services/`: Não identificado.

## 5. Decisões de Arquitetura

### ADR-0001 — `pyproject.toml` como fonte principal

* Status: Accepted.
* Centraliza metadados, dependências, pytest e Ruff.
* Evita usar `pip freeze` como definição principal de dependências.
* Estratégia de lockfile: Não identificada.

### ADR-0002 — Pacote central de observabilidade

* Status: Accepted.
* Logging centralizado em `app.core.observability`.
* Biblioteca padrão `logging` usada como base.
* `dictConfig` usado para configuração.
* `structlog` não foi adotado nesta fase.
* `apps/backend/app/core/logging.py` é uma implementação anterior sem imports ativos identificados.

### ADR-0003 — Middleware ASGI puro e `ContextVar`

* Status: Accepted.
* Middleware ASGI puro substituiu a abordagem com `BaseHTTPMiddleware`.
* `ContextVar` armazena o Request ID.
* `Token` restaura o contexto anterior.
* Header de correlação: `X-Request-ID`.

### ADR-0004 — Documentação como sistema de continuidade

* Status: Accepted.
* Ordem de retomada:

  1. `00_PROJECT_MASTER.md`
  2. `01_PROJECT_STATE.yaml`
  3. `02_BACKLOG.md`
  4. `03_CHANGELOG.md`
  5. ADRs
  6. `git status --short --branch`

### Outras decisões técnicas verificadas

* Modelo de negócio: Open Core.
* Direção de produto: Local First e Cloud Ready.
* Compatibilidade declarada no `pyproject.toml`: Python `>=3.12,<3.15`.
* Logs de desenvolvimento: formato de console estruturado.
* JSON logging está previsto pela configuração, mas não implementado.
* Formato de arquivo definido por `.editorconfig`: UTF-8, LF e newline final.
* Fluxo obrigatório declarado: pesquisa, análise, implementação, testes, validação, documentação e commit.

## 6. Dependências Instaladas

### Dependências diretas declaradas no `pyproject.toml`

* `fastapi>=0.139,<1.0`
* `uvicorn[standard]>=0.51,<1.0`
* `pydantic-settings>=2.14,<3.0`
* `orjson>=3.11,<4.0`

### Dependências de desenvolvimento declaradas

* `httpx>=0.28,<1.0`
* `pytest>=9.1,<10.0`
* `ruff>=0.15,<1.0`

### Pacotes instalados no ambiente virtual verificado

```text
alembic              1.18.5
annotated-doc        0.0.4
annotated-types      0.7.0
anyio                4.14.1
certifi              2026.6.17
click                8.4.2
colorama             0.4.6
detect-installer     0.1.0
dnspython            2.8.0
email-validator      2.3.0
fastapi              0.139.0
fastapi-cli          0.0.29
fastapi-cloud-cli    0.22.1
fastar               0.11.0
greenlet             3.5.3
h11                  0.16.0
httpcore             1.0.9
httptools            0.8.0
httpx                0.28.1
idna                 3.18
iniconfig            2.3.0
Jinja2               3.1.6
Mako                 1.3.12
markdown-it-py       4.2.0
MarkupSafe           3.0.3
mdurl                 0.1.2
orjson               3.11.9
packaging            26.2
pip                  26.1.2
pluggy               1.6.0
pydantic             2.13.4
pydantic_core        2.46.4
pydantic-extra-types 2.11.1
pydantic-settings    2.14.2
Pygments             2.20.0
pytest               9.1.1
python-dotenv        1.2.2
python-multipart     0.0.32
PyYAML               6.0.3
rich                 15.0.0
rich-toolkit         0.20.1
rignore              0.7.6
ruff                 0.15.21
sentry-sdk           2.64.0
setuptools           83.0.0
shellingham          1.5.4
SQLAlchemy           2.0.51
starlette            1.3.1
typer                0.26.8
typing_extensions    4.16.0
typing-inspection    0.4.2
urllib3              2.7.0
uvicorn              0.51.0
watchfiles           1.2.0
websockets           16.1
wheel                0.47.0
```

A adoção oficial de Alembic, SQLAlchemy e Sentry no código atual não foi identificada.

## 7. Próxima Sprint Recomendada

Não iniciar uma nova Sprint.

A recomendação é concluir a **SPRINT-02 — Logging System**, porque ela permanece em andamento e ainda não atende à Definition of Done.

Critérios mínimos para encerrar a SPRINT-02:

1. Implementar `JsonFormatter`.
2. Fazer `LOG_FORMAT` selecionar `console` ou `json`.
3. Eliminar a duplicação de `REQUEST_ID_HEADER`.
4. Remover ou consolidar `apps/backend/app/core/logging.py`.
5. Criar testes automatizados.
6. Corrigir as quatro violações Ruff `I001`.
7. Reexecutar pytest, Ruff, imports e testes HTTP.
8. Atualizar documentos de continuidade.
9. Criar commit específico da Sprint.
10. Confirmar publicação no repositório remoto.

## 8. Backlog Priorizado

### Prioridade 0 — Integridade do estado atual

1. Verificar novamente `git status --short --branch`.
2. Confirmar se as versões finais de `docs/00_PROJECT_MASTER.md` e `docs/01_PROJECT_STATE.yaml` estão staged.
3. Criar um commit separado para a documentação de continuidade.
4. Confirmar o push desse commit.
5. Não incluir o código incompleto da SPRINT-02 nesse commit documental.

### Prioridade 1 — Concluir o Logging System

1. Implementar `JsonFormatter`.
2. Conectar `settings.LOG_FORMAT` ao `dictConfig`.
3. Definir uma fonte única para `X-Request-ID`.
4. Remover o módulo legado `app/core/logging.py`.
5. Criar testes unitários para `request_context`.
6. Criar testes para `ConsoleFormatter` e `JsonFormatter`.
7. Criar testes de integração do middleware.
8. Testar geração automática de Request ID.
9. Testar preservação de Request ID recebido.
10. Testar inclusão do header na resposta.
11. Testar o caminho de exceção.
12. Corrigir Ruff.
13. Finalizar documentação e commit da Sprint.

### Prioridade 2 — Fundação do projeto

1. Preencher `README.md`.
2. Preencher `docs/research/2026-07-12-stack-tecnologica.md`.
3. Definir estratégia de lock de dependências.
4. Validar compatibilidade em Python 3.12, 3.13 e 3.14.
5. Definir CI para pytest e Ruff.
6. Documentar a arquitetura detalhada ainda ausente.

## 9. Dívida Técnica

* Nenhum teste automatizado coletado.
* Quatro violações Ruff `I001`.
* `LOG_FORMAT` declara `json`, mas o formato JSON não existe.
* `REQUEST_ID_HEADER` está declarado em `settings.py` e em `observability/constants.py`.
* `apps/backend/app/core/logging.py` não possui uso ativo identificado.
* `README.md` vazio.
* Documento de pesquisa tecnológica vazio.
* `pyproject.toml` ainda não commitado.
* Nenhum lockfile identificado.
* Compatibilidade com Python 3.12 e 3.13 não testada.
* Middleware não possui teste automatizado de concorrência.
* Caminho de exceção não foi validado.
* Request ID recebido não possui política identificada de validação ou limite de tamanho.
* CI/CD não identificado.
* Cobertura de testes não identificada.
* Tipagem estática com mypy/pyright não identificada.

## 10. Problemas Conhecidos

* `python -m pytest` retorna `no tests ran in 0.01s`.
* `python -m ruff check .` falha com quatro violações `I001` em:

  * `apps/backend/app/api/router.py`
  * `apps/backend/app/api/v1/health.py`
  * `apps/backend/app/core/observability/middleware.py`
  * `apps/backend/app/main.py`
* `LOG_FORMAT="json"` é aceito pelas configurações, mas não altera o formatter.
* `settings.REQUEST_ID_HEADER` não é a fonte usada pelo middleware; o middleware usa `observability.constants.REQUEST_ID_HEADER`.
* O módulo `apps/backend/app/core/logging.py` é redundante em relação ao pacote `observability`.
* O repositório possui mudanças staged, unstaged e untracked.
* O estado exato do staging dos dois documentos alterados por último não foi revalidado.
* PowerShell 5.1 pode introduzir UTF-8 BOM ao usar `Set-Content`; isso já causou falha de parsing no `pyproject.toml`.
* Nenhum problema persistente de latência foi reproduzido no endpoint testado.
* Banco de dados, agentes, memória, dashboard e integrações: Não identificado.

## 11. Arquivos Modificados desde o último commit

### Arquivos rastreados modificados

* `apps/backend/app/core/settings.py`
* `apps/backend/app/main.py`
* `docs/00_PROJECT_MASTER.md`

### Arquivos adicionados à documentação

* `docs/01_PROJECT_STATE.yaml`
* `docs/02_BACKLOG.md`
* `docs/03_CHANGELOG.md`
* `docs/adr/README.md`
* `docs/adr/ADR-0001-pyproject-como-fonte-de-dependencias.md`
* `docs/adr/ADR-0002-pacote-central-de-observabilidade.md`
* `docs/adr/ADR-0003-middleware-asgi-e-contextvars.md`
* `docs/adr/ADR-0004-documentacao-como-sistema-de-continuidade.md`

### Arquivos não rastreados identificados

* `apps/backend/app/core/logging.py`
* `apps/backend/app/core/observability/__init__.py`
* `apps/backend/app/core/observability/constants.py`
* `apps/backend/app/core/observability/filters.py`
* `apps/backend/app/core/observability/formatters.py`
* `apps/backend/app/core/observability/logging.py`
* `apps/backend/app/core/observability/middleware.py`
* `apps/backend/app/core/observability/request_context.py`
* `pyproject.toml`

### Estado de staging conhecido

* A documentação foi adicionada ao staging.
* `docs/03_CHANGELOG.md` foi atualizado no staging após correção de whitespace.
* `docs/00_PROJECT_MASTER.md` e `docs/01_PROJECT_STATE.yaml` foram alterados depois de uma validação de staging.
* Estado final desses dois arquivos no staging: Não identificado.

## 12. Próxima TASK recomendada

* **ID da Task:** Não identificado.
* **Título recomendado:** Consolidar e versionar o sistema de continuidade.
* **Objetivo:** garantir que a documentação final validada esteja staged, criar um commit exclusivamente documental e publicar esse commit sem incluir o código incompleto da SPRINT-02.
* **Primeira verificação a executar:**

```powershell
git status --short --branch
```

* **Depois da verificação:**

  1. comparar working tree e staging;
  2. adicionar novamente `docs/00_PROJECT_MASTER.md` e `docs/01_PROJECT_STATE.yaml` se necessário;
  3. executar `git diff --cached --check`;
  4. revisar `git diff --cached --stat`;
  5. criar commit documental;
  6. fazer push;
  7. retomar a SPRINT-02 sem iniciar nova Sprint.
